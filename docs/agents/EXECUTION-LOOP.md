# Execution Loop

This file points to the active execution contract:

- `AGENTS.md` — authority and repository rules.
- `WORKFLOW.md` — issue lifecycle, review gates, and merge handoff.

Run the active issue loop only through the live, active issue path and do not switch to
historical execution models for already-open active work.
