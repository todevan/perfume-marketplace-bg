# Retired builder state

> **Historical archive — non-authoritative.**
>
> Current owner decisions, live GitHub state, root `AGENTS.md`, and active
> repository contracts outrank every file on this shelf.

The retired `.builder` workspace is preserved byte-for-byte apart from Markdown
archive banners. No active repository tool or instruction references it. Its progress
and state files must not be treated as a queue, status ledger, or current product
truth.
